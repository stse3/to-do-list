export default class TodoLis {
    constructor (title,note, dueDate, priority){ //add something about proj ID
        this.title = title;
        this.note = note;
        this. dueDate = dueDate;
        this.priority= priority;

    }
    
}